const CONTENT_STATUS = {
    PENDING: "pending",
    SELECTED: "selected",
    REJECTED: "rejected",
};

const CONTENT_STATUS_ARRAY = Object.values(CONTENT_STATUS);

module.exports = {
    CONTENT_STATUS,
    CONTENT_STATUS_ARRAY
};